# Resume Builder Configuration

# Path to the default resume file (relative to /public directory)
# Examples:
# - /default-resume.md (markdown file in public folder)
# - /resumes/example.json (JSON file in public/resumes folder)
NEXT_PUBLIC_DEFAULT_RESUME_PATH=/default-resume.md
